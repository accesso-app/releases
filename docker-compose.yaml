version: "3.7"

volumes:
  image-storage:
    driver: local
    driver_opts:
      type: none
      device: "${PWD}/data/images"
      o: bind

  postgres-storage:
    driver: local
    driver_opts:
      type: none
      device: "${PWD}/data/postgres"
      o: bind

networks:
  entry:

services:
  database:
    container_name: authmenow-database
    image: postgres:11.5
    restart: always
    volumes:
      - "postgres-storage:/var/lib/postgresql/data"
    networks:
      - entry
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    expose:
      - "5432"

  public-api:
    container_name: authmenow-public-api
    image: docker.pkg.github.com/authmenow/backend/public-api:latest
    restart: always
    expose:
      - "9009"
    depends_on:
      - database
    networks:
      - entry
    environment:
      RUST_LOG: info
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # PREVIEW_QUEUE_URL: http://preview-queue:8080
      POSTGRES_DB: ${POSTGRES_DB}
      DATABASE_HOST: database
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database/${POSTGRES_DB}
      LISTEN_PORT: "9009"
      LISTEN_HOST: "0.0.0.0"
      SG_API_KEY: ${SENDGRID_API_KEY}
      SG_EMAIL_CONFIRM_URL_PREFIX: /register/confirm-
      SG_EMAIL_CONFIRM_TEMPLATE: d-eec45c55c0364140bf38172e021c8ea5

  frontend:
    container_name: authmenow-frontend
    image: docker.pkg.github.com/authmenow/frontend/main:latest
    expose:
      - "3000"
    networks:
      - entry
    environment:
      PUBLIC_URL: https://auth.local
      IMAGE_URL: https://auth.local/images
      BACKEND_URL: http://authmenow-public-api:9009

  facade:
    container_name: authmenow-facade
    image: docker.pkg.github.com/authmenow/releases/facade:latest
    ports:
      - 443:443
      - 80:80
    depends_on:
      - frontend
      - public-api
    networks:
      entry:
        aliases:
          - auth.local
    volumes:
      - "${PWD}/tls/authmenow.crt:/var/lib/authmenow/tls/authmenow.crt"
      - "${PWD}/tls/authmenow.pem:/var/lib/authmenow/tls/authmenow.pem"
      - "${PWD}/tls/dhparam.pem:/etc/nginx/ssl/dhparam.pem"
      - image-storage:/images
    environment:
      SERVER_NAME: auth.local
      MAIN_UPSTREAM: authmenow-frontend:3000
      BACKEND_UPSTREAM: authmenow-public-api:9009
  # admin:
  #   container_name: authmenow-admin
  #   image: docker.pkg.github.com/authmenow/admin/admin:latest
  #   networks:
  #     - entry
  #   expose: # ???
  #     - "80"
  #     - "443"
